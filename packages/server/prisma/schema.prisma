generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("PRISMA_DATABASE_URL")
}

model Proxy {
  ip String @id
}

model ProxyReservation {
  ip         String
  serviceId  String
  instanceId String

  @@id([ip, serviceId, instanceId])
}
